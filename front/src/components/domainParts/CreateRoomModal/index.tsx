import React from 'react'
import Component from './presentation'

const CreateRoomModal: React.FC = () => {
  return <Component open={true} handleClose={() => {}} />
}

export default CreateRoomModal
